- impostor-domain-model-and-types: added shared Impostor domain types for setup, round generation, reveals, runtime discussion state, results, and category word entries.
- impostor-settings-state-and-validation: refactored the Impostor setup into validated Svelte 5 state with normalized impostor/timer config, category validation, and reusable settings row components.
- impostor-round-setup-engine (Build the round setup and assignment engine): changed added a pure round setup engine in src/lib/games/impostor/round.ts with word selection, impostor assignment, hint generation, reveal payload creation, starting-player selection, and public exports in src/lib/index.ts; checks pnpm exec tsc --noEmit (fails on pre-existing src/hooks.ts implicit any and src/lib/paraglide/server.js async_hooks types), pnpm check (fails on pre-existing src/lib/paraglide/server.js async_hooks types), pnpm build (passes), pnpm exec prettier --check src/lib/games/impostor/round.ts src/lib/index.ts (tool not installed), jq empty prd.json (passes); commit message feat(impostor-round-setup-engine)-add: implement round setup engine; commit hash 492e5f3fffeaa95fe69e7a64a3364faf63b64f22
- impostor-secret-reveal-flow (Implement the per-player secret reveal interaction): changed added a dedicated `src/lib/games/impostor/reveal-phase.svelte` handoff screen, wired `/games/impostor` to create a round, gate `Nächster Spieler`/`Spiel starten` behind a first press-and-hold reveal, advance through every reveal card, and enter a minimal discussion-ready state after the last reveal; checks `pnpm check` (fails on pre-existing `src/lib/paraglide/server.js` missing `async_hooks` types), `pnpm build` (passes), tests/lint scripts not configured in `package.json`; commit message `feat(impostor-secret-reveal-flow)-add: implement secret reveal handoff`; commit hash `d5fe33923f4565e6dce6c8f790bad303118a03aa`
- impostor-live-round-timer-screen (Build the active game screen with circular countdown): changed added `src/lib/games/impostor/discussion-phase.svelte` for the live discussion phase with prominent starting-player callout, smooth circular countdown, timer/manual round ending, and wired `/games/impostor` into a shared results state that reveals impostors after manual end or timer expiry; checks `pnpm check` (fails on pre-existing `src/lib/paraglide/server.js` missing `async_hooks` types), `pnpm exec tsc --noEmit` (fails on pre-existing `src/hooks.ts` implicit any and `src/lib/paraglide/server.js` missing `async_hooks` types), `pnpm build` (passes), tests/lint scripts not configured in `package.json`; commit message `feat(impostor-live-round-timer-screen)-add: build circular discussion timer`; commit hash `4a1a08a4df3f241575248f49b120aadb317ed6a1`
- impostor-results-and-replay-flow (Show impostor results and enable starting the next game): changed added `src/lib/games/impostor/results-phase.svelte`, moved the route to a dedicated results screen, centralized round reset into shared replay setup, and wired `Nächstes Spiel` to regenerate word selection, impostor assignments, reveal state, and starting player while preserving setup choices; checks `pnpm exec tsc --noEmit` (fails on pre-existing `src/hooks.ts` implicit any and `src/lib/paraglide/server.js` missing `async_hooks` types), `pnpm check` (fails on pre-existing `src/lib/paraglide/server.js` missing `async_hooks` types), `pnpm build` (passes), tests/lint scripts not configured in `package.json`; commit message `feat(impostor-results-and-replay-flow)-add: implement results replay flow`; commit hash `1a6a596204b46d6f36be640a148004a4bfa77cbc`
- impostor-results-and-replay-flow correction: exact commit message `feat(impostor-results-and-replay-flow)-add: implement results replay flow`; exact commit hash `1a6a596bcd7ca9999a9f3848e2acbebdd937a43b`.
- impostor-route-composition-and-polish (Compose the route from small Svelte 5 components and polish edge cases): changed extracted the Impostor setup flow into `src/lib/games/impostor/settings-phase.svelte`, split the discussion timer into `src/lib/games/impostor/discussion-timer-circle.svelte`, simplified `src/routes/games/impostor/+page.svelte`, added a help dialog, privacy guidance, and stronger press-and-hold reveal handling with pointer-capture safeguards; checks `pnpm check` (fails on pre-existing `src/lib/paraglide/server.js` missing `async_hooks` types), `pnpm exec tsc --noEmit` (fails on pre-existing `src/hooks.ts` implicit any and `src/lib/paraglide/server.js` missing `async_hooks` types), `pnpm build` (passes), tests/lint scripts not configured in `package.json`; commit message `feat(impostor-route-composition-and-polish)-add: compose impostor route UI`; commit hash `abedf59167c3858e7159be81b9b22742bbe62078`
